# Operadores Lógicos

- **Comparação**: `==`, `===`, `!=`, `!==`, `>`, `<`, `>=`, `<=`.
- **Lógicos**: `&&` (e), `||` (ou), `!` (não).

Veja exemplos em `operadores-comparacao.js` e `operadores-logicos.js`. Pratique em `exercicios.md`.